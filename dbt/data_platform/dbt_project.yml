name: data_platform
version: "1.0.0"
config-version: 2

profile: data_platform

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  data_platform:
    silver:
      +materialized: view
      +schema: silver
    gold:
      +materialized: table
      +schema: gold

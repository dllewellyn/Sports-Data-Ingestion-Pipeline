-- Maps a canonical match to an ESPN event. Typed, empty scaffold.
select
    cast(null as varchar) as link_id,
    cast(null as varchar) as match_id,
    cast(null as varchar) as espn_event_id
limit 0

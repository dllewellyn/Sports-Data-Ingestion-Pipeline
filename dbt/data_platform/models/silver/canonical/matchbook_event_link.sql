-- Maps a canonical match to a Matchbook exchange event. Typed, empty scaffold.
select
    cast(null as varchar) as link_id,
    cast(null as varchar) as match_id,
    cast(null as varchar) as matchbook_event_id
limit 0
